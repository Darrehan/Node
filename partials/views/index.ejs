<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>EJS Tags</title>
</head>

<body>
  <h1>
    <%= title %>
  </h1>
  <p>Current second:
    <%= seconds %>
  </p>
  <% if (seconds % 2===0) { %>
    <ul>
      <% items.forEach((fruit)=> {%>
        <li>
          <%= fruit %>,<p>🤷‍♂️</p>
        </li>
        <% }); %>
    </ul>
    <%} else { %>
      <p>No items to display</p>
      <% } %>
        <p>
          <%- htmlContent %>
        </p>
<!-- partials is that section of code which we wana keep constant through out the project by using templating language -->
        <%- include("footer.ejs") %>
</body>

</html>